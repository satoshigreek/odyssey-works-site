# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Dependency / build dirs (none used today, but safe defaults)
node_modules/
.cache/

# Note: dist/ is tracked on purpose — it holds the portable single-file build.

# Logs
*.log
